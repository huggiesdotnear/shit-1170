{
  "name": "huggies_app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && rsbuild build",
    "typecheck": "tsc --noEmit",
    "dev": "rsbuild dev --open",
    "preview": "rsbuild preview",
    "web4_mainnet": "web4-deploy dist web4.huggies.near --nearfs"
  },
  "dependencies": {
    "fastintear": "^0.2.4",
    "preact": "^10.27.1",
    "preact-router": "^4.1.2",
    "ref-sdk": "^0.1.5"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.5.0",
    "@rsbuild/plugin-preact": "^1.5.2",
    "typescript": "^5.9.2"
  }
}
